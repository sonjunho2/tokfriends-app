# REST API Base URL
TOK_API_BASE=https://tok-friends-api.onrender.com

# WebSocket Base URL (서버에 WS 주소 없으면 우선 REST만 둬도 됨)
TOK_WS_BASE=wss://tok-friends-api.onrender.com

# JWT 저장 키 (앱 내부에서 사용)
TOK_JWT_STORAGE_KEY=tokfriends.jwt
